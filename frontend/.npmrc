# Use legacy peer deps algorithm for dependency resolution
# This is required for Angular 19 compatibility with current packages
legacy-peer-deps=true

# Other npm configurations
engine-strict=true
save-exact=false
